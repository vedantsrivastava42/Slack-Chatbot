# Required
SLACK_BOT_TOKEN=xoxb-your-bot-token
SLACK_APP_TOKEN=xapp-your-app-token
GEMINI_API_KEY=your-gemini-api-key

# Optional: ClickUp-integrated channel — bot auto-answers new messages (task title + description) in thread
# Set to Slack channel ID (e.g. C0AH7795CBX). Leave unset to disable.
# CLICKUP_SLACK_CHANNEL_ID=C0AH7795CBX

# Optional
ENABLE_READONLY_ENFORCEMENT=true
MAX_CONVERSATION_MESSAGES=10
# GEMINI_MODEL=models/gemini-flash-latest
# CURSOR_AGENT_MODEL=auto
